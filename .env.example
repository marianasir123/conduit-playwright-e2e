# ─── Application ───────────────────────────────────────────────────────────────
# Base URL of the application under test.
# Override this to point at a staging or local environment.
BASE_URL=https://conduit.bondaracademy.com

# ─── Test Credentials ──────────────────────────────────────────────────────────
# A dedicated test account — never use production credentials here.
# Create the account once through the UI or via the API before running tests.
TEST_EMAIL=your-test-email@example.com
TEST_PASSWORD=your-secure-password
